/sumo-webapp
│
├── /app                     # FastAPI Backend (Python files)
│   ├── __init__.py
│   ├── main.py              # FastAPI main app (handles routes)
│   └── /static              # Optional: static files like images, CSS, JS
│       └── style.css        # For frontend custom styles (optional)
│
├── /uploads                 # Folder for storing uploaded files (ZIPs)
│   └── example.zip          # Example file uploaded during tests (optional)
│
├── /frontend                # Frontend (HTML, JS, CSS files)
│   └── index.html           # Main HTML file to upload ZIP and trigger simulation
│
├── Dockerfile               # Docker configuration for containerizing app
├── requirements.txt         # List of dependencies for the backend
└── README.md                # Project documentation (optional)